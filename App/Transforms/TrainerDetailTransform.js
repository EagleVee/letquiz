import TrainerTransform from "./TrainerTransform";

export default class TrainerDetailTransform extends TrainerTransform {
  constructor(params) {
    super(params);
    // this.appendFields(params);
  }
}
